(() => {
  const banner = `\n------------------------------------------------------------\nJS Learning Console\nOpen DevTools to watch each chapter log examples.\nTimestamp: ${new Date().toLocaleString()}\n------------------------------------------------------------`;
  console.log(banner);
})();
